# Agent Notes (password_generator)

- Think in English, but respond in Spanish (all assistant-facing output).
- Never use emojis anywhere: not in responses, commands, code, or comments.
- This workspace currently only contains `planning.md` (no `package.json`, `src/`, Vite config, tests, or CI). Before attempting any build/test/lint commands, first confirm the repo is fully checked out/synced.
- The only verified source of truth here is `planning.md` (Spanish).
- `planning.md` explicitly says: `No implementes todavía.` Do not start implementation work unless the user overrides this.
- `planning.md` claims the intended target repo is `https://github.com/TOB1EH/Password-Generator.git` and that it uses Vue 3 + Vite; treat that as unverified until the actual project files are present.
